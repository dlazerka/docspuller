ToDo:
	f step-by-step working (Stop -> Pause, project saving, def_project.ini)
	f relative pathes in UI
	f bad characters in urls (and conflicted filenames)
	f validating project settings
	ñ clarify PageStatus
	


Page statuses:
	failed regexp
	queued
	fetching...
	fetching failed
	fetched
	stored
	parsed

