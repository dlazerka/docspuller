ToDo:
	>f show urls which doesn't satisfy project's RegExp (maybe checkbox)
	f numbering of pages
	f step-by-step working (Stop -> Pause, project saving)
	f relative pathes in UI
	f bad characters in urls (and conflicted filenames)
	ñ clarify PageStatus


Page statuses:
	queued
	fetching...
	failed
	fetched
	stored
	parsed

